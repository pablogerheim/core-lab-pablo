
description
price

cor -> selec 
options 
branco
preto
cinza
vermelho
azul
verde
laranja
amarelo
marron
rosa
roxo

terminar FIlters
 
Mudar o Add para form de formulario 

            body:  {
                "name": name,
                "description": description,
                "plate": plate,
                "year": year,
                "color": "azul",
                "price": price
            }

           JSON.stringify({ name, color, description, year, plate, price })

                       headers: {
                "Content-Type": "application/json",
                "Access-Control-Allow-Origin": "*",
                "Access-Control-Allow-Credentials": "true"
            },

                function selec(text:string) {
        let helperArr:string[] =[]
       
        Object.entries(filters).forEach((arr) => {arr[0]===text? helperArr = arr[1] : console.log()})

        return(
            <label className="w-full" key={text}> 
            <p className="pl-8">{upFirstLetter(text)}:</p>
                <select className="p-2 pl-8 font-medium w-full border rounded-3xl border-black" >
                    {helperArr.map((e: string, i) => <option key={v4()} >{e}</option>)}        
                </select>
            </label>
        )
    }