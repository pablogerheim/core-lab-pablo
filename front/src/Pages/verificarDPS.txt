import { AiOutlineArrowLeft } from 'react-icons/ai';
import { upFirstLetter } from "../Helper/HelperFunction";
import { CAR_INFO, COLORS } from "../Helper/HelperContents";
import { useState } from 'react';
import { IVehicleAdd } from '../../Types/Vehicle';

ter uma função que vai pencher cada campo do de data para fazer a requisição

function Add() {
    const [data, setData] = useState<IVehicleAdd|{}>({})
    
    function handleSubmit(params: any): void {

    }

    function handleImput(params:string) {

        
        setData()
    }

    function field(text: string) {

        return (
            <label className="w-full">
                <p className="pl-8">{upFirstLetter(text)}:</p>
                <input
                    className="p-2 pl-8 font-medium w-full border rounded-3xl border-black"
                    placeholder={upFirstLetter(text)}
                    onChange={(e) => handleImput(e.target.value)}
                />
            </label>
        )
    }
    function select() {
        return (<select className='p-2 pl-8  font-medium w-full border rounded-3xl border-black'>
            <option value='' selected></option>
            {Object.keys(COLORS).map((key, value) => <option value={value} key={key}>{upFirstLetter(key)}</option>)}
        </select>)
    }
    return (
        <div className="p-4 bg-slate-200 h-screen" >
            <a href='/'><AiOutlineArrowLeft className='h-6 w-8' /></a>
            <form onSubmit={handleSubmit} className="m-6 mt-4  bg-white">
                <div className=" pt-[3rem] items-center p-4 ">
                    {CAR_INFO.map(e => field(e))}
                    <label className='mt-2'>
                        <p className="pl-8">Cor:</p>
                        {select()}</label>
                </div>
                <div className="flex justify-end mt-8">
                    <button type='submit' className="border rounded-3xl m-10 px-8 p-2 bg-green-300">
                        <p>SALVAR</p>
                    </button>
                </div>
            </form>
        </div>
    );
}

export { Add };